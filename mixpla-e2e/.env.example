# Target environment (optional; defaults to https://mixpla.io)
BASE_URL=https://mixpla.io

# Submitter identity for the track-submission flow: an email plus its
# one-time code. Use the QA OTP bypass pair (OtpService.TEST_BYPASS_EMAIL /
# TEST_BYPASS_CODE in datanest), which verifies without sending a real email.
MIXDECK_USER=qa-test@mixpla.io
MIXDECK_PWD=424242
